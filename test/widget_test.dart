// Real widget tests will be added as features stabilize. The default
// counter-app test from `flutter create` was removed because it referenced
// a `MyApp` widget that no longer exists in this project.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
